{
  "name": "badbankbackend",
  "version": "1.0.0",
  "description": "badbankbackend",
  "engines": {
    "node": "14.17.0"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm run install-client && npm run build",
    "client": "cd ./client && npm start"
  },
  "author": "ramziosta@gmail.com",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.1",
    "google-auth-library": "^7.14.1",
    "js-cookie": "^3.0.1",
    "jsonwebtoken": "^8.5.1",
    "mongodb": "^4.5.0",
    "nodemon": "^2.0.16"
  }
}
